strict-peer-dependencies=false
auto-install-peers=true
node-linker=hoisted

# NOTE: nodeLinker also lives in pnpm-workspace.yaml for pnpm 11 compatibility.
# For pnpm 9 (EAS Build server), it is read from this .npmrc file.
